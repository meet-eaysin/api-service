// import { departmentController } from '@/modules/department';
// import { Router } from 'express';

// const departmentRoute = Router();

// departmentRoute.route('/').get(departmentController.getAll).post(departmentController.create);

// departmentRoute
//   .route('/:id')
//   .get(departmentController.queryById)
//   .put(departmentController.upsert)
//   .patch(departmentController.partialUpdate)
//   .delete(departmentController.remove);

// export default departmentRoute;
